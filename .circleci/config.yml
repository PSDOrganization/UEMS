version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:16

    steps:
      - checkout

      # Install dependencies
      - run:
          name: Install Node.js dependencies
          command: npm install

      # Run tests
      - run:
          name: Run tests
          command: npm test

workflows:
  version: 2
  test:
    jobs:
      - build
